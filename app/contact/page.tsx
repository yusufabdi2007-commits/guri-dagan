import ContactForm from '@/components/contact/ContactForm';

export const metadata = {
  title: 'Contact — Guri Dagan',
  description: 'Get in touch with Coach Rahma for parent and child coaching.',
};

export default function ContactPage() {
  return <ContactForm />;
}
